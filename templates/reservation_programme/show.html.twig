{% extends 'base.html.twig' %}

{% block title %}ReservationProgramme{% endblock %}

{% block body %}
    <h1>ReservationProgramme</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reservation_programme.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ reservation_programme.date ? reservation_programme.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Nbpersonne</th>
                <td>{{ reservation_programme.nbpersonne }}</td>
            </tr>
            <tr>
                <th>Heure</th>
                <td>{{ reservation_programme.heure }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reservation_programme_index') }}">back to list</a>

    <a href="{{ path('app_reservation_programme_edit', {'id': reservation_programme.id}) }}">edit</a>

    {{ include('reservation_programme/_delete_form.html.twig') }}
{% endblock %}
