<form method="post" action="{{ path('app_reservation_programme_delete', {'id': reservation_programme.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation_programme.id) }}">
    <button class="btn">Delete</button>
</form>
